# -*- mode: python ; coding: utf-8 -*-

# Base automatically generated by
# pyinstaller --noconfirm --windowed --clean --name=Nexus --icon=../nexus-icon.icns ../../../runnexus.py

a = Analysis(
    ['../../../runnexus.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='Nexus',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['../nexus-icon.icns'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='Nexus',
)
app = BUNDLE(
    coll,
    name='Nexus.app',
    icon='../nexus-icon.icns',
    bundle_identifier=None,
    info_plist={
        'CFBundleShortVersionString':'0.9.0',
        'CFBundleIdentifier':'energy.entropy.nexus',
        'CFBundleDocumentTypes': [
            {
                'CFBundleTypeName':'A Nexus mindmap',
                'CFBundleTypeRole':'Viewer',
                'CFBundleTypeExtensions':['nex'],
                'CFBundleTypeIconFile':'nexus-icon.icns',
            }],
        'CFBundleURLTypes': [
            {
                'CFBundleURLName':'Nexus map',
                'CFBundleURLSchemes':['nexus'],
            }],
        },
)
